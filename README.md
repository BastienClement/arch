# Bastien Clément's Arch Repository (bclm)

## Installing

Add the following to `/etc/pacman.conf`:

```
[bclm]
SigLevel = Optional TrustAll
Server = https://bastienclement.github.io/arch/x86_64
```
